<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <style name="ValueTextView">
        <item name="android:textSize">@dimen/value_font_size</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="TemperatureText">
        <item name="android:gravity">center</item>
        <item name="android:padding">4dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="TemperatureText.Max">
        <item name="android:background">@drawable/background_max_temp</item>
    </style>

    <style name="TemperatureText.Min">
        <item name="android:background">@drawable/background_min_temp</item>
    </style>


</resources>
